
import java.util.ArrayList;
import java.util.Random;
import java.util.Scanner;
public class Randome {

    public static void main(String[] args) {
        Random rand = new Random();
        Scanner sc = new Scanner(System.in);

        //20 elemanlı bir dizi oluştur.
        //Diziyi 0–50 arasında rastgele sayılarla doldur
        //Bu dizideki tekrar eden sayıları bul ve ekrana yazdır.
        //Ayrıca tekrar eden sayıların kaç kez geçtiğini göster
        
 int[]dizi=new int[20];
 ArrayList<String> dizi1=new ArrayList<>();
 for(int i=0;i<dizi.length;i++){
     
    
    dizi[i]=rand.nextInt(50);
 

  } for(int sayi:dizi){
System.out.println(""+sayi);
int adet=0;
for(int i=0;i<20;i++){
    if(dizi[i]==sayi){
        adet++;
    }
}
       dizi1.add(String.valueOf(adet));
       adet=0;
}
boolean kontrol=true;
for(int i=0;i<20;i++){
    for(int c=0;c<i;c++){
        if(i==c){
            kontrol=false;
        }
    }
    if(kontrol==false){
        continue;
    }
    System.out.println(dizi[i]+"-dan"+dizi1.get(i)+"adet var");
}
 
    }
}
